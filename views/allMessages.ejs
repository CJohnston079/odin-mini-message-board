<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Pet Message Board | Messages</title>
		<script src="./scripts/allMessages.js" defer></script>
		<link rel="stylesheet" href="./styles/index.css" />
		<link rel="stylesheet" href="./styles/allMessages.css" />
		<link rel="shortcut icon" href="./favicon.svg" type="image/x-icon" />
	</head>
	<body>
		<div id="message-board">
			<header class="page-header-container">
				<h1 class="page-heading">Pet Message Board</h1>
				<p class="page-subheading">Leave a message from your pet!</p>
			</header>
			<div class="gradient-overlay"></div>
			<section id="message-board-container">
				<div id="messages-container">
					<% messages.map((message) => { %>
					<article class="message-container">
						<header class="message-header-container">
							<h3 class="message-username"><%= message.username %></h3>
							<time class="date-added" datetime="<%= message.sent_at.toISOString() %>">
								<%= formatRelativeTime(message.sent_at) %>
							</time>
						</header>
						<p class="message-body"><%= message.message %></p>
					</article>
					<% }) %>
				</div>
				<p id="messages-end-text">End of messages</p>
			</section>
			<nav>
				<a class="btn-emphasis" href="./new">
					<span class="btn-emphasis-text">Leave a message +</span>
				</a>
			</nav>
		</div>
	</body>
</html>
